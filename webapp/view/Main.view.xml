<mvc:View controllerName="my.namespace.controller.Main" xmlns="sap.m" xmlns:core="sap.ui.core" xmlns:mvc="sap.ui.core.mvc" xmlns:semantic="sap.m.semantic"
	xmlns:table="sap.ui.table" xmlns:custom="my.namespace.customcontrol">
	<semantic:FullscreenPage id="page" title="{i18n>mainTitle}" height="100%" enableScrolling="false">
		<semantic:content>
			<OverflowToolbar>
				<ToolbarSpacer />
				<Label text="Upload button invisible" />
				<Switch id="uploadInvisibleSwitch"
					state="false"
					change="onSwitchUploadInvisibleChange"
					type="Transparent" />
				<Label text="Layout: Enable FileUploader" />
				<Switch state="true"
					change="onSwitchUploaderChange"
					type="Transparent" />
				<Label text="Multi-selection mode" />
				<Switch id="modeSwitch"
					state="false"
					change="onSwitchModeChange"
					type="Transparent" />
				<Label text="File types" />
				<MultiComboBox id="fileTypesBox"
					selectionChange="onFileTypeChange"
					width="250px"
					placeholder="Choose the valid file types"
					items="{fileTypes>/items}">
					<core:Item key="{fileTypes>key}" text="{fileTypes>text}" />
				</MultiComboBox>
			</OverflowToolbar>
			<UploadCollection id="UploadCollection"
				maximumFilenameLength="55"
				maximumFileSize="10"
				multiple="true"
				showSeparators="true"
				change="onChange"
				fileDeleted="onFileDeleted"
				filenameLengthExceed="onFilenameLengthExceed"
				fileRenamed="onFileRenamed"
				fileSizeExceed="onFileSizeExceed"
				selectionChange="onSelectionChange"
				typeMissmatch="onTypeMissmatch"
				uploadComplete="onUploadComplete"
				beforeUploadStarts="onBeforeUploadStarts"
				uploadTerminated="onUploadTerminated"
				items="{path : '/items', templateShareable : 'true'}"
				numberOfAttachmentsText="Uploaded (8)"
				mode="SingleSelectMaster">
				<toolbar>
					<OverflowToolbar id="myId">
						<Title id="attachmentTitle" />
						<ToolbarSpacer />
						<Button id="deleteSelectedButton"
							text="Delete"
							press="onDeleteSelectedItems"
							enabled="false"
							visible="false"
							type="Transparent" />
						<ToggleButton id="selectAllButton"
							text="Select all"
							press="onSelectAllPress"
							enabled="false"
							visible="false"
							type="Transparent" />
						<UploadCollectionToolbarPlaceholder />
					</OverflowToolbar>
				</toolbar>
				<items>
					<UploadCollectionItem documentId="{documentId}"
						fileName="{fileName}"
						mimeType="{mimeType}"
						thumbnailUrl="{thumbnailUrl}"
						url="{url}"
						enableEdit="{enableEdit}"
						enableDelete="{enableDelete}"
						visibleDelete="{visibleDelete}"
						visibleEdit="{visibleEdit}"
						attributes="{path : 'attributes', templateShareable : 'true'}"
						statuses="{path : 'statuses', templateShareable : 'true'}"
						markers="{path : 'markers', templateShareable : 'true'}"
						selected="{selected}">
						<attributes>
							<ObjectAttribute title="{title}"
								text="{path : 'text', formatter : '.formatAttribute'}"
								active="{active}"
								press="onAttributePress" />
						</attributes>
						<statuses>
							<ObjectStatus title="{title}"
								text="{text}"
								state="{state}"
								icon="{icon}"
								iconDensityAware="{iconDensityAware}"
								textDirection="{textDirection}" />
						</statuses>
						<markers>
							<ObjectMarker type="{type}" press="onMarkerPress" />
						</markers>
					</UploadCollectionItem>
				</items>
			</UploadCollection>
		</semantic:content>
	</semantic:FullscreenPage>
</mvc:View>